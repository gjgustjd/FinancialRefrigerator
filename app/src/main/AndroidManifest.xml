<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.study.financialrefrigerator">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".common.GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
        <activity
            android:name=".presentation.refrigerator.ingredients.IngredientsActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.activity.search.SearchRecipesActivity"
            android:exported="false" />
        <activity
            android:name="com.study.financialrefrigerator.presentation.recipe.detail.RecipeDetailActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activity.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>